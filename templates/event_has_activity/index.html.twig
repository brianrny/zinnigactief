{% extends 'base.html.twig' %}

{% block title %}EventHasActivity index{% endblock %}

{% block body %}
    <h1>EventHasActivity index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for event_has_activity in event_has_activities %}
            <tr>
                <td>{{ event_has_activity.id }}</td>
                <td>
                    <a href="{{ path('event_has_activity_show', {'id': event_has_activity.id}) }}">show</a>
                    <a href="{{ path('event_has_activity_edit', {'id': event_has_activity.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('event_has_activity_new') }}">Create new</a>
{% endblock %}
