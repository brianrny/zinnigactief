{% extends 'base.html.twig' %}

{% block title %}EventHasActivity{% endblock %}

{% block body %}
    <h1>EventHasActivity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ event_has_activity.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('event_has_activity_index') }}">back to list</a>

    <a href="{{ path('event_has_activity_edit', {'id': event_has_activity.id}) }}">edit</a>

    {{ include('event_has_activity/_delete_form.html.twig') }}
{% endblock %}
